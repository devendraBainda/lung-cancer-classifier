services:
  - type: web
    name: lung-cancer-classifier
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    branch: main
    autoDeploy: true
